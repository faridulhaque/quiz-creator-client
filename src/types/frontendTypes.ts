export type authType = {
    setLogin: (value: boolean) => void
}


export type regInfoType = {
    email: string
    password: string
    name: string
}
export type loginInfoType = {
    email: string
    password: string
}